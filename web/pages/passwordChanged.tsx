import { Header } from 'semantic-ui-react';
import Layout from '../components/layout';

const PasswoedChanged = () => {
    return (
        <Layout>
            <Header textAlign="center" color="teal">Password has been changed successfully</Header>
        </Layout>
    )
}
export default PasswoedChanged;